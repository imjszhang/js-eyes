# Changelog

## 0.1.0

- 首个版本：`repo` / `issues` / `issue` 三 profile；READ：`getRepo`、`listIssues`、`getIssue`、`sessionState`；INTERACTIVE：`navigateRepo`、`navigateIssues`、`navigateIssue`；开发者 CLI：`dom-dump`、`xhr-log`。
