'use strict';

/**
 * Canonical browser-operation metadata.
 *
 * Names, routing, permissions, policy identity, input schemas, and host tool
 * titles live here. Hosts adapt transport/result formatting only.
 */

const TARGET_PROP = Object.freeze({
  type: 'string',
  minLength: 1,
  maxLength: 200,
  description: 'Browser clientId or unique browser name.',
});

const TAB_ID_PROP = Object.freeze({
  type: 'integer',
  minimum: 0,
  description: 'Browser tab ID.',
});

const TIMEOUT_PROP = Object.freeze({
  type: 'number',
  exclusiveMinimum: 0,
  maximum: 1800,
  description: 'Operation timeout in seconds.',
});

function objectSchema(properties, required = [], extras = {}) {
  const schema = /** @type {Record<string, any>} */ ({
    type: 'object',
    properties: Object.freeze(properties),
    ...(required.length ? { required: Object.freeze(required.slice()) } : {}),
    additionalProperties: false,
  });
  if (extras.anyOf) {
    schema.anyOf = Object.freeze(extras.anyOf.map((item) => Object.freeze({
      ...item,
      ...(Array.isArray(item.required)
        ? { required: Object.freeze(item.required.slice()) }
        : {}),
    })));
  }
  return Object.freeze(schema);
}

const BROWSER_OPERATIONS = Object.freeze([
  {
    id: 'tabs.list',
    wireAction: 'get_tabs',
    sdkMethod: 'getTabs',
    openclawAction: 'browser/get-tabs',
    mcpTool: 'browser_list_tabs',
    capability: 'browser.tabs.read',
    risk: 'read',
    routing: 'server',
    profiles: Object.freeze(['safe', 'full']),
    title: 'JS Eyes: List Tabs',
    label: 'JS Eyes: Get Tabs',
    description: 'List open browser tabs. Without target, tabs from all connected browser clients are returned.',
    openclawDescription: '获取浏览器中所有已打开的标签页列表，包含每个标签页的 ID、URL、标题等信息。',
    inputSchema: objectSchema({
      target: { ...TARGET_PROP, description: '目标浏览器的 clientId 或名称（如 firefox、chrome）。省略则返回所有浏览器的标签页。' },
      timeout: TIMEOUT_PROP,
    }),
    annotations: Object.freeze({ readOnly: true, idempotent: true }),
  },
  {
    id: 'clients.list',
    wireAction: 'list_clients',
    sdkMethod: 'listClients',
    openclawAction: 'browser/list-clients',
    mcpTool: 'browser_list_clients',
    capability: 'browser.tabs.read',
    risk: 'read',
    routing: 'server',
    profiles: Object.freeze(['safe', 'full']),
    title: 'JS Eyes: List Browser Clients',
    label: 'JS Eyes: List Clients',
    description: 'List browser clients connected to the local JS Eyes server.',
    openclawDescription: '获取当前已连接到 JS-Eyes 服务器的浏览器客户端列表。',
    inputSchema: objectSchema({}),
    annotations: Object.freeze({ readOnly: true, idempotent: true }),
  },
  {
    id: 'url.open',
    wireAction: 'open_url',
    sdkMethod: 'openUrl',
    policyTool: 'openUrl',
    openclawAction: 'browser/open-url',
    mcpTool: 'browser_open_url',
    capability: 'browser.navigation',
    risk: 'interactive',
    routing: 'extension',
    profiles: Object.freeze(['safe', 'full']),
    title: 'JS Eyes: Open URL',
    label: 'JS Eyes: Open URL',
    description: 'Open a URL in a new tab or navigate an existing tab. JS Eyes egress policy applies.',
    openclawDescription: '在浏览器中打开指定 URL。可以打开新标签页，也可以在已有标签页中导航。返回标签页 ID。',
    inputSchema: objectSchema({
      url: { type: 'string', format: 'uri', maxLength: 8192, description: '要打开的 URL' },
      tabId: { ...TAB_ID_PROP, description: '已有标签页 ID（传入则在该标签页导航，省略则新开标签页）' },
      windowId: { type: 'integer', minimum: 0, description: '窗口 ID（新开标签页时可指定窗口）' },
      target: TARGET_PROP,
      timeout: TIMEOUT_PROP,
    }, ['url']),
    annotations: Object.freeze({ openWorld: true }),
  },
  {
    id: 'tab.close',
    wireAction: 'close_tab',
    sdkMethod: 'closeTab',
    openclawAction: 'browser/close-tab',
    mcpTool: 'browser_close_tab',
    capability: 'browser.navigation',
    risk: 'interactive',
    routing: 'extension',
    profiles: Object.freeze(['safe', 'full']),
    title: 'JS Eyes: Close Tab',
    label: 'JS Eyes: Close Tab',
    description: 'Close a browser tab.',
    openclawDescription: '关闭浏览器中指定 ID 的标签页。',
    inputSchema: objectSchema({
      tabId: TAB_ID_PROP,
      target: TARGET_PROP,
      timeout: TIMEOUT_PROP,
    }, ['tabId']),
    annotations: Object.freeze({ destructive: true, idempotent: true }),
  },
  {
    id: 'page.html',
    wireAction: 'get_html',
    sdkMethod: 'getTabHtml',
    openclawAction: 'browser/get-html',
    mcpTool: 'browser_get_html',
    capability: 'browser.page.read',
    risk: 'read',
    routing: 'extension',
    profiles: Object.freeze(['safe', 'full']),
    title: 'JS Eyes: Get Page HTML',
    label: 'JS Eyes: Get HTML',
    description: 'Read HTML from a browser tab. Output is truncated to the requested character limit.',
    openclawDescription: '获取指定标签页的完整 HTML 内容。',
    inputSchema: objectSchema({
      tabId: TAB_ID_PROP,
      target: TARGET_PROP,
      timeout: TIMEOUT_PROP,
      maxChars: { type: 'integer', minimum: 1000, maximum: 1000000 },
    }, ['tabId']),
    annotations: Object.freeze({ readOnly: true, idempotent: true }),
  },
  {
    id: 'script.execute',
    wireAction: 'execute_script',
    sdkMethod: 'executeScript',
    policyTool: 'executeScript',
    openclawAction: 'browser/execute-script',
    mcpTool: 'browser_execute_script',
    capability: 'browser.script.execute',
    risk: 'destructive',
    routing: 'extension',
    sensitive: true,
    profiles: Object.freeze(['full']),
    title: 'JS Eyes: Execute JavaScript',
    label: 'JS Eyes: Execute Script',
    description: 'Execute JavaScript in a browser tab. This is a high-risk full-profile tool.',
    openclawDescription: '在指定标签页中执行 JavaScript 代码并返回执行结果。可用于提取页面数据、操作 DOM 等。',
    inputSchema: objectSchema({
      tabId: TAB_ID_PROP,
      code: { type: 'string', minLength: 1, maxLength: 200000, description: '要执行的 JavaScript 代码' },
      target: TARGET_PROP,
      timeout: TIMEOUT_PROP,
    }, ['tabId', 'code']),
    annotations: Object.freeze({ destructive: true, openWorld: true }),
  },
  {
    id: 'style.inject',
    wireAction: 'inject_css',
    sdkMethod: 'injectCss',
    policyTool: 'injectCss',
    openclawAction: 'browser/inject-css',
    mcpTool: 'browser_inject_css',
    capability: 'browser.css.inject',
    risk: 'interactive',
    routing: 'extension',
    sensitive: true,
    profiles: Object.freeze(['full']),
    title: 'JS Eyes: Inject CSS',
    label: 'JS Eyes: Inject CSS',
    description: 'Inject CSS into a browser tab. This is a high-risk full-profile tool.',
    openclawDescription: '向指定标签页注入自定义 CSS 样式。可用于隐藏页面元素、调整布局等。',
    inputSchema: objectSchema({
      tabId: TAB_ID_PROP,
      css: { type: 'string', minLength: 1, maxLength: 200000, description: '要注入的 CSS 代码' },
      target: TARGET_PROP,
      timeout: TIMEOUT_PROP,
    }, ['tabId', 'css']),
    annotations: Object.freeze({ destructive: true }),
  },
  {
    id: 'cookies.read',
    wireAction: 'get_cookies',
    sdkMethod: 'getCookies',
    policyTool: 'getCookies',
    openclawAction: 'browser/get-cookies',
    mcpTool: 'browser_get_cookies',
    capability: 'browser.cookies.read',
    risk: 'read',
    routing: 'extension',
    sensitive: true,
    profiles: Object.freeze(['full']),
    title: 'JS Eyes: Get Cookies',
    label: 'JS Eyes: Get Cookies',
    description: 'Read cookies for a browser tab. This sensitive tool is available only in the full profile.',
    openclawDescription: '获取指定标签页对应域名的所有 Cookie。',
    inputSchema: objectSchema({
      tabId: TAB_ID_PROP,
      target: TARGET_PROP,
      timeout: TIMEOUT_PROP,
    }, ['tabId']),
    annotations: Object.freeze({ readOnly: true, idempotent: true }),
  },
  {
    id: 'cookies.readDomain',
    wireAction: 'get_cookies_by_domain',
    sdkMethod: 'getCookiesByDomain',
    policyTool: 'getCookiesByDomain',
    openclawAction: 'browser/get-cookies-by-domain',
    mcpTool: 'browser_get_cookies_by_domain',
    capability: 'browser.cookies.read',
    risk: 'read',
    routing: 'extension',
    sensitive: true,
    profiles: Object.freeze(['full']),
    title: 'JS Eyes: Get Cookies By Domain',
    label: 'JS Eyes: Get Cookies By Domain',
    description: 'Read cookies by domain. This sensitive tool is available only in the full profile.',
    openclawDescription: '按域名获取浏览器中的所有 Cookie，无需指定标签页。支持包含子域名。',
    inputSchema: objectSchema({
      domain: { type: 'string', minLength: 1, maxLength: 253, description: "目标域名（如 'example.com'）" },
      includeSubdomains: { type: 'boolean', description: '是否包含子域名的 Cookie（默认 true）' },
      target: TARGET_PROP,
      timeout: TIMEOUT_PROP,
    }, ['domain']),
    annotations: Object.freeze({ readOnly: true, idempotent: true }),
  },
  {
    id: 'cookies.write',
    wireAction: 'set_cookies',
    sdkMethod: 'setCookies',
    policyTool: 'setCookies',
    capability: 'browser.cookies.write',
    risk: 'destructive',
    routing: 'extension',
    sensitive: true,
    profiles: Object.freeze(['full']),
    title: 'JS Eyes: Set Cookies',
    label: 'JS Eyes: Set Cookies',
    description: 'Write cookies on a browser connector. Intended for server-side sync and SDK tests; cookie values must not be echoed in logs.',
    inputSchema: objectSchema({
      cookies: {
        type: 'array',
        minItems: 1,
        maxItems: 500,
        description: 'Cookie records to write (name/value/domain/path and flags).',
        items: {
          type: 'object',
          additionalProperties: false,
          properties: {
            name: { type: 'string', minLength: 1, maxLength: 4096 },
            value: { type: 'string', maxLength: 4096 },
            domain: { type: 'string', minLength: 1, maxLength: 253 },
            path: { type: 'string', maxLength: 2048 },
            secure: { type: 'boolean' },
            httpOnly: { type: 'boolean' },
            sameSite: {
              type: 'string',
              enum: Object.freeze(['strict', 'lax', 'none', 'unspecified']),
            },
            expirationDate: { type: 'number' },
            expires: { type: 'number' },
            session: { type: 'boolean' },
          },
          required: ['name', 'value'],
        },
      },
      overwrite: {
        type: 'string',
        enum: Object.freeze(['merge', 'replace']),
        description: 'merge (default) overwrites matching cookies; replace deletes extras for the domain first.',
      },
      domain: { type: 'string', minLength: 1, maxLength: 253, description: 'Domain used when overwrite is replace.' },
      target: TARGET_PROP,
      timeout: TIMEOUT_PROP,
    }, ['cookies']),
    annotations: Object.freeze({ destructive: true }),
  },
  {
    id: 'cookies.sync',
    wireAction: 'sync_cookies',
    sdkMethod: 'syncCookies',
    policyTool: 'syncCookies',
    openclawAction: 'browser/sync-cookies',
    mcpTool: 'browser_sync_cookies',
    capability: 'browser.cookies.write',
    capabilities: Object.freeze(['browser.cookies.read', 'browser.cookies.write']),
    risk: 'destructive',
    routing: 'server',
    sensitive: true,
    profiles: Object.freeze(['full']),
    title: 'JS Eyes: Sync Cookies',
    label: 'JS Eyes: Sync Cookies',
    description: 'Copy cookies for one domain between two connected browsers in server memory. The response is counts only and never includes cookie values.',
    openclawDescription: '按域名在两个已连接浏览器之间同步 Cookie。服务端内存中转，结果只返回 copied/skipped 计数，不含 Cookie 值。',
    inputSchema: objectSchema({
      domain: { type: 'string', minLength: 1, maxLength: 253, description: "目标域名（如 'example.com'）" },
      includeSubdomains: { type: 'boolean', description: '是否包含子域名的 Cookie（默认 true）' },
      source: { ...TARGET_PROP, description: '源浏览器 clientId 或唯一 browserName' },
      destination: { ...TARGET_PROP, description: '目标浏览器 clientId 或唯一 browserName' },
      overwrite: {
        type: 'string',
        enum: Object.freeze(['merge', 'replace']),
        description: 'merge（默认）覆盖同名 Cookie；replace 先删除目标域上源中不存在的 Cookie。',
      },
      timeout: TIMEOUT_PROP,
    }, ['domain', 'source', 'destination']),
    annotations: Object.freeze({ destructive: true }),
  },
  {
    id: 'page.info',
    wireAction: 'get_page_info',
    sdkMethod: 'getPageInfo',
    openclawAction: 'browser/get-page-info',
    mcpTool: 'browser_get_page_info',
    capability: 'browser.page.read',
    risk: 'read',
    routing: 'extension',
    profiles: Object.freeze(['safe', 'full']),
    title: 'JS Eyes: Get Page Info',
    label: 'JS Eyes: Get Page Info',
    description: 'Read URL, title, status, and other metadata from a browser tab.',
    openclawDescription: '获取指定标签页的页面信息，包括 URL、标题、状态和图标。',
    inputSchema: objectSchema({
      tabId: TAB_ID_PROP,
      target: TARGET_PROP,
      timeout: TIMEOUT_PROP,
    }, ['tabId']),
    annotations: Object.freeze({ readOnly: true, idempotent: true }),
  },
  {
    id: 'file.upload',
    wireAction: 'upload_file_to_tab',
    sdkMethod: 'uploadFileToTab',
    policyTool: 'uploadFileToTab',
    openclawAction: 'browser/upload-file',
    mcpTool: 'browser_upload_file',
    capability: 'browser.files.upload',
    risk: 'destructive',
    routing: 'extension',
    sensitive: true,
    profiles: Object.freeze(['full']),
    title: 'JS Eyes: Upload File',
    label: 'JS Eyes: Upload File',
    description: 'Upload base64-encoded files through a page file input. Available only in the full profile.',
    openclawDescription: '向指定标签页的文件上传控件上传文件。文件以 Base64 编码传入，自动设置到页面的 file input 元素。',
    inputSchema: objectSchema({
      tabId: TAB_ID_PROP,
      files: {
        type: 'array',
        minItems: 1,
        maxItems: 20,
        description: '要上传的文件列表',
        items: {
          type: 'object',
          properties: {
            base64: { type: 'string', minLength: 1, maxLength: 20000000, description: '文件内容的 Base64 编码' },
            name: { type: 'string', minLength: 1, maxLength: 255, description: '文件名' },
            type: { type: 'string', minLength: 1, maxLength: 255, description: "MIME 类型（如 'image/png'）" },
          },
          required: ['base64', 'name', 'type'],
          additionalProperties: false,
        },
      },
      targetSelector: {
        type: 'string',
        minLength: 1,
        maxLength: 2000,
        description: '目标 file input 的 CSS 选择器（默认 input[type="file"]）',
      },
      target: TARGET_PROP,
      timeout: TIMEOUT_PROP,
    }, ['tabId', 'files']),
    annotations: Object.freeze({ destructive: true }),
  },
  {
    id: 'screenshot.capture',
    wireAction: 'capture_screenshot',
    sdkMethod: 'captureScreenshot',
    mcpTool: 'browser_take_screenshot',
    capability: 'browser.screenshot',
    risk: 'read',
    routing: 'extension',
    profiles: Object.freeze(['safe', 'full']),
    title: 'JS Eyes: Take Screenshot',
    label: 'JS Eyes: Take Screenshot',
    description: 'Capture a browser tab and return native MCP image content.',
    openclawDescription: '截取指定标签页的屏幕截图。',
    inputSchema: objectSchema({
      tabId: TAB_ID_PROP,
      target: TARGET_PROP,
      timeout: TIMEOUT_PROP,
      format: { type: 'string', enum: Object.freeze(['png', 'jpeg']) },
      quality: { type: 'integer', minimum: 0, maximum: 100 },
      fullPage: { type: 'boolean' },
    }, ['tabId']),
    annotations: Object.freeze({ readOnly: true, idempotent: true }),
  },
  {
    id: 'page.click',
    wireAction: 'click',
    sdkMethod: 'click',
    openclawAction: 'browser/click',
    mcpTool: 'browser_click',
    capability: 'browser.page.interact',
    risk: 'interactive',
    routing: 'extension',
    profiles: Object.freeze(['safe', 'full']),
    title: 'JS Eyes: Click Element',
    label: 'JS Eyes: Click',
    description: 'Click a page element by CSS selector, XPath, visible text, or a page.state ref. Does not require allowRawEval.',
    openclawDescription: '通过 CSS 选择器、XPath、可见文本或 page.state ref 点击页面元素。不依赖 allowRawEval。',
    inputSchema: objectSchema({
      tabId: TAB_ID_PROP,
      selector: { type: 'string', maxLength: 2000, description: 'CSS selector or XPath' },
      text: { type: 'string', maxLength: 2000, description: 'Optional visible text match' },
      ref: { type: 'string', minLength: 1, maxLength: 32, description: 'Opaque element ref from page.state' },
      index: { type: 'integer', minimum: 0, description: 'Match index when multiple elements match' },
      target: TARGET_PROP,
      timeout: TIMEOUT_PROP,
    }, ['tabId'], {
      anyOf: [
        { required: ['selector'] },
        { required: ['text'] },
        { required: ['ref'] },
      ],
    }),
    annotations: Object.freeze({ openWorld: true }),
  },
  {
    id: 'page.fill',
    wireAction: 'fill',
    sdkMethod: 'fill',
    policyTool: 'fill',
    openclawAction: 'browser/fill',
    mcpTool: 'browser_fill',
    capability: 'browser.page.interact',
    risk: 'interactive',
    routing: 'extension',
    profiles: Object.freeze(['safe', 'full']),
    title: 'JS Eyes: Fill Form Field',
    label: 'JS Eyes: Fill',
    description: 'Fill an input, textarea, select, or contenteditable element. Accepts a selector or page.state ref, and a value or secretRef. Does not require allowRawEval.',
    openclawDescription: '填写 input/textarea/select 或 contenteditable。可用 selector/ref，以及 value 或 secretRef。不依赖 allowRawEval。',
    inputSchema: objectSchema({
      tabId: TAB_ID_PROP,
      selector: { type: 'string', minLength: 1, maxLength: 2000 },
      ref: { type: 'string', minLength: 1, maxLength: 32 },
      value: { type: 'string', maxLength: 100000 },
      secretRef: { type: 'string', minLength: 1, maxLength: 64, description: 'Name of a local secret; value is resolved on the server and never echoed.' },
      clearFirst: { type: 'boolean' },
      index: { type: 'integer', minimum: 0 },
      target: TARGET_PROP,
      timeout: TIMEOUT_PROP,
    }, ['tabId'], {
      anyOf: [
        { required: ['selector', 'value'] },
        { required: ['selector', 'secretRef'] },
        { required: ['ref', 'value'] },
        { required: ['ref', 'secretRef'] },
      ],
    }),
    annotations: Object.freeze({ openWorld: true }),
  },
  {
    id: 'page.scroll',
    wireAction: 'scroll',
    sdkMethod: 'scroll',
    openclawAction: 'browser/scroll',
    mcpTool: 'browser_scroll',
    capability: 'browser.page.interact',
    risk: 'interactive',
    routing: 'extension',
    profiles: Object.freeze(['safe', 'full']),
    title: 'JS Eyes: Scroll Page',
    label: 'JS Eyes: Scroll',
    description: 'Scroll the page to top/bottom, by pixels, or to an element. Does not require allowRawEval.',
    openclawDescription: '滚动到顶部/底部、按像素滚动，或滚动到指定元素。不依赖 allowRawEval。',
    inputSchema: objectSchema({
      tabId: TAB_ID_PROP,
      scrollTarget: {
        type: 'string',
        enum: Object.freeze(['top', 'bottom']),
        description: 'Scroll to top or bottom when selector/pixels omitted',
      },
      selector: { type: 'string', maxLength: 2000 },
      ref: { type: 'string', minLength: 1, maxLength: 32 },
      pixels: { type: 'number' },
      target: TARGET_PROP,
      timeout: TIMEOUT_PROP,
    }, ['tabId']),
    annotations: Object.freeze({ openWorld: true }),
  },
  {
    id: 'page.waitFor',
    wireAction: 'wait_for',
    sdkMethod: 'waitFor',
    openclawAction: 'browser/wait-for',
    mcpTool: 'browser_wait_for',
    capability: 'browser.page.interact',
    risk: 'interactive',
    routing: 'extension',
    profiles: Object.freeze(['safe', 'full']),
    title: 'JS Eyes: Wait For Condition',
    label: 'JS Eyes: Wait For',
    description: 'Wait until a selector, ref, load, URL substring, or title substring matches. Does not require allowRawEval.',
    openclawDescription: '等待选择器、ref、load、URL 或标题条件满足。不依赖 allowRawEval。',
    inputSchema: objectSchema({
      tabId: TAB_ID_PROP,
      condition: {
        type: 'string',
        enum: Object.freeze(['selector', 'ref', 'load', 'url', 'title']),
        description: 'Wait condition (default selector)',
      },
      selector: { type: 'string', minLength: 1, maxLength: 2000 },
      ref: { type: 'string', minLength: 1, maxLength: 32 },
      match: { type: 'string', maxLength: 2000, description: 'Substring for url or title conditions' },
      timeout: { type: 'number', exclusiveMinimum: 0, maximum: 1800, description: 'Wait timeout in seconds' },
      visible: { type: 'boolean' },
      networkIdle: { type: 'boolean', description: 'CDP-only extra settle signal; other connectors ignore it.' },
      target: TARGET_PROP,
    }, ['tabId']),
    annotations: Object.freeze({ openWorld: true }),
  },
  {
    id: 'page.state',
    wireAction: 'get_page_state',
    sdkMethod: 'getPageState',
    openclawAction: 'browser/page-state',
    mcpTool: 'browser_page_state',
    capability: 'browser.page.read',
    risk: 'read',
    routing: 'extension',
    profiles: Object.freeze(['safe', 'full']),
    title: 'JS Eyes: Page State',
    label: 'JS Eyes: Page State',
    description: 'Snapshot interactive elements as short-lived refs for click/fill. Does not require allowRawEval.',
    openclawDescription: '采集当前页可交互元素并返回短生命周期 ref，供后续 click/fill 使用。',
    inputSchema: objectSchema({
      tabId: TAB_ID_PROP,
      maxElements: { type: 'integer', minimum: 1, maximum: 500 },
      interactiveOnly: { type: 'boolean' },
      target: TARGET_PROP,
      timeout: TIMEOUT_PROP,
    }, ['tabId']),
    annotations: Object.freeze({ readOnly: true }),
  },
  {
    id: 'page.keys',
    wireAction: 'send_keys',
    sdkMethod: 'sendKeys',
    openclawAction: 'browser/send-keys',
    mcpTool: 'browser_send_keys',
    capability: 'browser.page.interact',
    risk: 'interactive',
    routing: 'extension',
    profiles: Object.freeze(['safe', 'full']),
    title: 'JS Eyes: Send Keys',
    label: 'JS Eyes: Send Keys',
    description: 'Send a whitelisted key or modifier chord. Optional ref focuses the target first.',
    openclawDescription: '发送白名单按键或修饰键组合。可选 ref 先聚焦目标。',
    inputSchema: objectSchema({
      tabId: TAB_ID_PROP,
      keys: { type: 'string', minLength: 1, maxLength: 64 },
      ref: { type: 'string', minLength: 1, maxLength: 32 },
      target: TARGET_PROP,
      timeout: TIMEOUT_PROP,
    }, ['tabId', 'keys']),
    annotations: Object.freeze({ openWorld: true }),
  },
  {
    id: 'page.history',
    wireAction: 'navigate_history',
    sdkMethod: 'navigateHistory',
    openclawAction: 'browser/history',
    mcpTool: 'browser_history',
    capability: 'browser.navigation',
    risk: 'interactive',
    routing: 'extension',
    profiles: Object.freeze(['safe', 'full']),
    title: 'JS Eyes: History',
    label: 'JS Eyes: History',
    description: 'Navigate back or forward in tab history.',
    openclawDescription: '在标签页历史中前进或后退。',
    inputSchema: objectSchema({
      tabId: TAB_ID_PROP,
      direction: { type: 'string', enum: Object.freeze(['back', 'forward']) },
      target: TARGET_PROP,
      timeout: TIMEOUT_PROP,
    }, ['tabId', 'direction']),
    annotations: Object.freeze({ openWorld: true }),
  },
  {
    id: 'page.select',
    wireAction: 'select_option',
    sdkMethod: 'selectOption',
    openclawAction: 'browser/select',
    mcpTool: 'browser_select',
    capability: 'browser.page.interact',
    risk: 'interactive',
    routing: 'extension',
    profiles: Object.freeze(['safe', 'full']),
    title: 'JS Eyes: Select Option',
    label: 'JS Eyes: Select',
    description: 'Select a native <select> option by value or label. Use a selector or page.state ref.',
    openclawDescription: '按 value 或 label 选择原生 select 选项。',
    inputSchema: objectSchema({
      tabId: TAB_ID_PROP,
      selector: { type: 'string', minLength: 1, maxLength: 2000 },
      ref: { type: 'string', minLength: 1, maxLength: 32 },
      value: { type: 'string', maxLength: 2000 },
      label: { type: 'string', maxLength: 2000 },
      index: { type: 'integer', minimum: 0 },
      target: TARGET_PROP,
      timeout: TIMEOUT_PROP,
    }, ['tabId'], {
      anyOf: [
        { required: ['selector'] },
        { required: ['ref'] },
      ],
    }),
    annotations: Object.freeze({ openWorld: true }),
  },
  {
    id: 'page.dialog',
    wireAction: 'handle_dialog',
    sdkMethod: 'handleDialog',
    openclawAction: 'browser/handle-dialog',
    mcpTool: 'browser_handle_dialog',
    capability: 'browser.page.interact',
    risk: 'interactive',
    routing: 'extension',
    profiles: Object.freeze(['safe', 'full']),
    title: 'JS Eyes: Handle Dialog',
    label: 'JS Eyes: Handle Dialog',
    description: 'Accept or dismiss a JavaScript dialog. Supported on CDP/BiDi; the extension returns CAPABILITY_UNSUPPORTED.',
    openclawDescription: '接受或关闭 JavaScript 对话框。扩展连接器可能返回 CAPABILITY_UNSUPPORTED。',
    inputSchema: objectSchema({
      tabId: TAB_ID_PROP,
      action: { type: 'string', enum: Object.freeze(['accept', 'dismiss']) },
      promptText: { type: 'string', maxLength: 4000 },
      target: TARGET_PROP,
      timeout: TIMEOUT_PROP,
    }, ['tabId', 'action']),
    annotations: Object.freeze({ openWorld: true }),
  },
  {
    id: 'downloads.list',
    wireAction: 'list_downloads',
    sdkMethod: 'listDownloads',
    policyTool: 'listDownloads',
    openclawAction: 'browser/list-downloads',
    mcpTool: 'browser_list_downloads',
    capability: 'browser.files.download',
    risk: 'read',
    routing: 'extension',
    sensitive: true,
    profiles: Object.freeze(['full']),
    title: 'JS Eyes: List Downloads',
    label: 'JS Eyes: List Downloads',
    description: 'List recent browser downloads as metadata only (basename, state, bytes). No file contents or full paths.',
    openclawDescription: '列出最近的浏览器下载元数据，不含文件内容和完整路径。',
    inputSchema: objectSchema({
      tabId: TAB_ID_PROP,
      target: TARGET_PROP,
      timeout: TIMEOUT_PROP,
    }),
    annotations: Object.freeze({ readOnly: true }),
  },
  {
    id: 'downloads.wait',
    wireAction: 'wait_download',
    sdkMethod: 'waitDownload',
    policyTool: 'waitDownload',
    openclawAction: 'browser/wait-download',
    mcpTool: 'browser_wait_download',
    capability: 'browser.files.download',
    risk: 'read',
    routing: 'extension',
    sensitive: true,
    profiles: Object.freeze(['full']),
    title: 'JS Eyes: Wait Download',
    label: 'JS Eyes: Wait Download',
    description: 'Wait for a download to complete. Returns metadata only.',
    openclawDescription: '等待下载完成，只返回元数据。',
    inputSchema: objectSchema({
      id: { type: 'string', minLength: 1, maxLength: 128 },
      basename: { type: 'string', minLength: 1, maxLength: 255 },
      tabId: TAB_ID_PROP,
      timeout: { type: 'number', exclusiveMinimum: 0, maximum: 1800, description: 'Wait timeout in seconds' },
      target: TARGET_PROP,
    }),
    annotations: Object.freeze({ openWorld: true }),
  },
  {
    id: 'page.waitForUser',
    wireAction: 'wait_for_user',
    sdkMethod: 'waitForUser',
    openclawAction: 'browser/wait-for-user',
    mcpTool: 'browser_wait_for_user',
    capability: 'browser.page.interact',
    risk: 'interactive',
    routing: 'server',
    profiles: Object.freeze(['safe', 'full']),
    title: 'JS Eyes: Wait For User',
    label: 'JS Eyes: Wait For User',
    description: 'Pause until the operator resumes from the extension popup or `js-eyes browser resume`. Use for login or 2FA.',
    openclawDescription: '暂停等待操作者在扩展 popup 或 CLI resume 后继续。用于登录/2FA。',
    inputSchema: objectSchema({
      reason: { type: 'string', minLength: 1, maxLength: 500 },
      tabId: TAB_ID_PROP,
      timeout: { type: 'number', exclusiveMinimum: 0, maximum: 1800, description: 'Wait timeout in seconds (default 300)' },
      target: TARGET_PROP,
    }, ['reason']),
    annotations: Object.freeze({ openWorld: true }),
  },
  {
    id: 'page.extract',
    wireAction: 'extract_page',
    sdkMethod: 'extractPage',
    policyTool: 'extractPage',
    openclawAction: 'browser/extract-page',
    mcpTool: 'browser_extract_page',
    capability: 'browser.page.read',
    risk: 'read',
    routing: 'extension',
    profiles: Object.freeze(['safe', 'full']),
    title: 'JS Eyes: Extract Page',
    label: 'JS Eyes: Extract Page',
    description: 'Extract structured page content (markdown/text/html) without raw eval. Does not require allowRawEval.',
    openclawDescription: '以声明式方式提取页面正文与元数据，不依赖 allowRawEval。',
    inputSchema: objectSchema({
      tabId: TAB_ID_PROP,
      format: { type: 'string', enum: Object.freeze(['markdown', 'text', 'html']) },
      includeLinks: { type: 'boolean' },
      includeImages: { type: 'boolean' },
      maxContentChars: { type: 'integer', minimum: 1, maximum: 2000000 },
      maxLinks: { type: 'integer', minimum: 0, maximum: 500 },
      maxImages: { type: 'integer', minimum: 0, maximum: 200 },
      target: TARGET_PROP,
      timeout: TIMEOUT_PROP,
    }, ['tabId']),
    annotations: Object.freeze({ readOnly: true, idempotent: true }),
  },
].map((operation) => Object.freeze(operation)));

const BROWSER_OPERATION_BY_WIRE_ACTION = Object.freeze(Object.fromEntries(
  BROWSER_OPERATIONS.map((operation) => [operation.wireAction, operation]),
));

const BROWSER_OPERATION_BY_ID = Object.freeze(Object.fromEntries(
  BROWSER_OPERATIONS.map((operation) => [operation.id, operation]),
));

const BROWSER_OPERATION_BY_OPENCLAW_ACTION = Object.freeze(Object.fromEntries(
  BROWSER_OPERATIONS
    .filter((operation) => operation.openclawAction)
    .map((operation) => [operation.openclawAction, operation]),
));

const BROWSER_OPERATION_BY_MCP_TOOL = Object.freeze(Object.fromEntries(
  BROWSER_OPERATIONS
    .filter((operation) => operation.mcpTool)
    .map((operation) => [operation.mcpTool, operation]),
));

// routing: 'extension' means "send to a browser connector" for this cycle
// (extension, CDP, or BiDi). Do not add a silent `browser` routing alias.
const FORWARDABLE_ACTIONS = Object.freeze(BROWSER_OPERATIONS
  .filter((operation) => operation.routing === 'extension')
  .map((operation) => operation.wireAction));

const SENSITIVE_BROWSER_ACTIONS = Object.freeze(BROWSER_OPERATIONS
  .filter((operation) => operation.sensitive)
  .map((operation) => operation.wireAction));

const SENSITIVE_BROWSER_TOOL_NAMES = Object.freeze(BROWSER_OPERATIONS
  .filter((operation) => operation.sensitive && operation.openclawAction)
  .map((operation) => operation.openclawAction));

const ACTION_POLICY_TOOL_MAP = Object.freeze(Object.fromEntries(
  BROWSER_OPERATIONS
    .filter((operation) => operation.policyTool)
    .map((operation) => [operation.wireAction, operation.policyTool]),
));

function listBrowserOperationsForProfile(profile = 'safe') {
  const normalized = profile === 'full' ? 'full' : 'safe';
  return BROWSER_OPERATIONS.filter((operation) => (operation.profiles || []).includes(normalized));
}

module.exports = {
  ACTION_POLICY_TOOL_MAP,
  BROWSER_OPERATIONS,
  BROWSER_OPERATION_BY_ID,
  BROWSER_OPERATION_BY_MCP_TOOL,
  BROWSER_OPERATION_BY_OPENCLAW_ACTION,
  BROWSER_OPERATION_BY_WIRE_ACTION,
  FORWARDABLE_ACTIONS,
  SENSITIVE_BROWSER_ACTIONS,
  SENSITIVE_BROWSER_TOOL_NAMES,
  listBrowserOperationsForProfile,
};
